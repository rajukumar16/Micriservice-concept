server:
  port: 8085
spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/rajukumar16/microservice-config.git
          default-label: main
          clone-on-start: true
